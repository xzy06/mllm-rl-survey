# 二稿_英文版.docx 质检报告

生成方式：机翻（Google 翻译网页接口为主，MyMemory 兜底），译文原样保留；本报告所列"已修正"项均为**词汇级替换或机械格式修复**，未做整句改写。仍存在的问题列出供你手动定夺。

---

## 一、漏译检查 ✅ 无

- 182 个中文文本块全部已译（182/182），无空段。
- 文内引用标注 `[1]`–`[23]` 与原文逐一比对，全部保留、无错位。
- 数字换算正确：`37 万→370,000`、`5 万→50,000`、`18.5 万→1.89 万→185,000 → 18,900`；`2025 年 3 月→March 2025`（非漏译）。
- 24 个公式（OMML）、4 个表格、图1 及其题注、23 条英文参考文献均原样保留。

---

## 二、已修正的格式问题（机械修复）

| 问题 | 位置 | 处理 |
|---|---|---|
| 公式两侧空格丢失 | 7 个含行内公式段（POLIA、其中、H-GRPO、双层优势等） | 按原文块边界补回空格；4 段原文本身即无空格处保持与原文一致 |
| 加粗标签丢失 | `Keywords`、3 处 `Section summary:` | 恢复加粗，与二稿一致 |
| 空格前置标点 | `Section 3 .` | → `Section 3.` |
| 上标/符号被插入空格 | `CR ³`×2、`Q-Spatial + +`、`V *` | → `CR³`、`Q-Spatial++`、`V*` |
| 专有名词大小写 | `Polia`×4、`Mme`、`THE METHOD` | → `POLIA`、`MME`、`Method` |
| 箭头间距不统一 | 6 处 `→` | 统一为 ` → ` |
| 残留中文标点 | 公式间 2 处 `、` | → `, ` |
| MyMemory 接口伪影 | 2 处 `&None` | → `None` |

---

## 三、已修正的术语/词汇问题（词汇级统一）

| 中文 | 修正前（多种） | 统一为 | 依据 |
|---|---|---|---|
| 组合推理 | combinatorial reasoning / combinatorial inference / combination reasoning / combined reasoning | **compositional reasoning** | 领域标准术语；原文括注即用此词 |
| 组合性/组合结构/组合绑定等 | combinatorial / combination + 各名词 | compositional + 各名词 | 同上 |
| 验证器 | verifier / validator 混用（3.1 标题 Verifier、正文 Validator） | **verifier** | RLVR 标准用法 |
| 结果验证 | result verification / result validation | **result verification** | 同上 |
| 基座模型 | base model / pedestal model | **base model** | pedestal 系机翻错译 |
| 思维链 | thought chain / thinking chain | **chain-of-thought** | 标准用法 |
| 策略(梯度) | strategy / strategy gradient | **policy / policy gradient** | RL 标准用法（PPO=Policy Optimization） |
| 场景图 | scene map / scene icons / scene labeling | **scene graph** | 标准用法 |
| 显著性 | significance level | **saliency level** | 视觉显著性 |
| 通用 LVLM | Universal LVLM | **general LVLM** | 通用=general |
| 接地 | Ground / Ground IoU | **grounding / grounded IoU** | 标准用法 |
| 节内小结 | Section summary / Summary of the section | **Section summary** | 统一 |
| 奖励 | reward / bonus / Benefits | **reward / advantage** | 统一 |
| 论文 | thesis | **paper** | — |
| 提升 | promotion | **improvement** | — |
| 对比预训练 | comparative pre-training | **contrastive pre-training** | 标准用法 |
| 领域外退化 | out-of-field degradation | **out-of-domain degradation** | 标准用法 |
| 共指 | core-reference | **coreference** | — |
| 脑补 | make up for it | **hallucinate** | — |
| 蒙对 | mislead | **guess correctly** | — |
| 沿用 | Inherit | **Following** | — |
| 连续谱 | sequential spectra | **continuous spectrum** | — |

**定点词修正**（保留原句结构，仅换词）：

- 标题：`combinatorial inference` → `compositional reasoning`
- `[10:0]` 贡献段三类方法名乱序 `visual pairs Optimize the reward and inference process` → `visual alignment reward and reasoning process optimization`（恢复三类目名称）
- `[51:0]/[71:0]/[82:0]` 三分法 `"whether it is correct", "whether it is correct"` → `"whether it is correct", "whether it looks at the right evidence"`（恢复 是否正确 / 是否看对 的区分，原译两处相同丢失核心区分）
- `[68:0]/[77:0]` 公式前 `in` → `where`（数学惯用法）
- `[63:0]` `mislead the combination task` → `guess correctly on compositional tasks`
- `[173:0]` `Mirror Flip Q&A Correct` → `Mirror-flip Q&A pairs`
- `[179:0]` `More standard GRPO` → `vs. standard GRPO`
- `[187:0]` `VSR et al. 7 benchmarks` → `7 benchmarks incl. VSR`
- `[188:0]` `Two-Layer Advantage` → `Two-level advantage`
- `[203:0]` 章节标题 `4. Explore` → `4. Discussion`
- `[213:0]` `Split verifiable rewards` → `Decomposed verifiable rewards`
- `[217:0]` 结论 `this paper combines the representative work` → `this paper reviews the representative work`
- `[200:0]` 注 `of the thesis` → `of the paper`；`"promotion"` → `"improvement"`
- `[211:0]` `just blinded` → `just guessed correctly`；`looked at the graph` → `looked at the image`

---

## 四、仍存在的翻译问题（建议手动定夺）

以下属**句级断裂或需专业判断**，未自动改写（按你"词汇级修改"的要求保留原句）：

1. **`[9:0]` 引言贡献段断句错乱**：`...from 2024 to 2026 After representative work...`（"After" 大写、句子在 2026 处断裂）；`(Self-Questioning) VLM, H-GRPO)` 括号错位；`...the final answer. Reward sources (CR³...)` 句号错位。
2. **`[15:0]` 断句**：`...easily hacked For the problem, LLM is used...`（"For" 大写、断点不对）。
3. **`[205:0]` 4.1 节开头**：`Section 3.4 trisections are conveniently described`（三分法在描述上是方便的）——句法生硬；`Polia's external and internal advantages coexist as a loss function`（同处一个损失函数→coexist as a loss function 表意不准）。
4. **`[200:0]` 注释句结构错乱**：`Different method bases and training data are different from the evaluation protocol`（各方法基座、训练数据与评测协议不同 → 结构被译乱）。
5. **`[211:0]`** ：`the result verification method cannot distinguish between "really looked at the image" and "just guessed correctly"` 尚可，但 `artificial scene labeling` 已改 `manual scene-graph labeling`；`frameless supervision`（无框监督）—— "frameless" 略怪，可考虑 `box-free supervision`。
6. **`[179:0]`** ：`V*` 后 mech 已修为 `V*`，但 `V* +2.1` 表述仍简略（原文：较标准 GRPO 在 V* 上 +2.1）。
7. **`[27:0]`** ：`Strong existence`（存在性强）—— 评测语境下 "existence" 可读性一般，可改 `Strong on existence`。
8. **`[35:0]/[40:0]`** ：表1 内 `检索` 一词两译——CREPE 行 `Retrieval`、ARO 行 `Search`；`二选一检索` → `Choose one search`（应为 two-choice retrieval）。属术语一致性，但表格内零散，建议手动统一为 `Retrieval`。
9. **`[217:0]`** ：`strengthening MLLM compositional reasoning with reinforcement learning` —— "strengthening" 略生硬，可改 `enhancing`。
10. **`[106:0]`** ：`Evaluation does not explicitly cover combinations` —— "combinations" 复数偏怪，可改 `compositional cases`。
11. **`[43:0]`** 标题 `2.2 Main line of RL foundation and reward signal evolution` —— "Main line" 可读性差，可改 `2.2 RL background and the reward signal evolution thread`。
12. **`[82:0]`** 末句 `categories are not closed drawers`（类别不是封闭的抽屉）—— 直译保留机翻味，可接受。

---

## 五、仍存在的格式小问题

- 表格表头大小写不统一：表3 多为小写（`method`/`signaling mechanism`），表4 多为首字母大写（`Benchmark`/`Key Data/Design`）。原文表头格式一致，系两套机翻引擎（gtx vs MyMemory）输出风格差异。建议手动统一为词首大写或全小写。
- `万级` 译法三种并存：`Level 370,000` / `Level 50,000` / `7.5 thousand level`。建议统一为 `370K-level` 风格或全写。
- `无` 在表中大小写不一：`none` / `None`。建议统一。
- 表4 `[155:0]` `THE METHOD` 已改 `Method`，但与表3 `method` 仍大小写不同。
- 箭头 `→` 前后空格虽已统一为 ` → `，但少数表格单元格内 `→` 紧贴数字（如 `0.349 → 0.387`）排版上略宽，可酌情改为 `-` 或保留。

---

## 六、非翻译问题提醒

- 作者署名 `南方科技大学 工学院，广东 珠海 519000` 来自中文原稿。南方科技大学主校区在深圳，珠海 519000 为珠海邮编——若署名有误请在**中文二稿**一并修正（英文版会同步）。此非翻译问题。
- `席致远 → Xi Zhiyuan` 拼音正确。

---

## 附：本轮修改统计

- 术语统一影响 **61 个文本块**；定点词修正 **约 20 处**。
- 格式机械修复：公式空格 7 段、加粗标签 4 处、大小写/上标/箭头/标点共约 20 处。
- 修订脚本：`tmp/fix_translations.py`（词汇修正）、`tmp/build_en_v2.py`（含 mech_fix 重建）。可复用重跑。
